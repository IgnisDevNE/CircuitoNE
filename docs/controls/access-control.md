# Controle de acesso à entrega e à infraestrutura

Estado verificado em 22/09/2026. Este controle cobre GitHub, esteira, Supabase e hospedagem; as permissões dos **usuários do site** são tratadas nas [regras de negócio](../business-rules/mvp.md) e serão implementadas com Auth/RLS. A [#31](https://github.com/IgnisDevNE/CircuitoNE/issues/31) acompanha a separação ainda incompleta entre agente e mantenedor. Uma permissão instalada é o **limite máximo que a chave pode solicitar**; restringir o token de um comando não restringe a própria chave ([GitHub](https://docs.github.com/en/apps/creating-github-apps/authenticating-with-a-github-app/authenticating-as-a-github-app-installation)).

## Identidades e autoridade atual

| Identidade | Acesso necessário no fluxo | Limite e situação verificada |
|---|---|---|
| `magalz` (mantenedor humano) | Administrar repositórios, Apps, ambientes e credenciais; revisar QA, homologação e releases. Acessa o dashboard Supabase com sua conta pessoal. | A proteção do CircuitoNE impede aprovar a própria PR e o environment protegido impede autoaprovação de deployment. A sessão GitHub, as credenciais humanas e a conexão Supabase da conta pessoal ainda estão acessíveis pelo ambiente do agente; isso impede afirmar isolamento forte. O papel exato no Supabase não foi revalidado nesta auditoria. |
| App implementador `ignisdevne` (5028495) | Criar branch/commit/PR e issues no CircuitoNE; consultar checks e execuções. | Instalação **selected repositories**, atualmente apenas `IgnisDevNE/CircuitoNE`; não acessa `CircuitoNE-QA`. O helper `scripts/github-app.mjs` emite token somente para o CircuitoNE, normalmente sem `Workflows:write` e com `Actions:read`. Não possui Administration, Secrets ou Environments. |
| App `CircuitoNE QA Publisher` (5037798) | Abrir PR de proposta no QA e publicar `canonical-acceptance` no CircuitoNE. | Instalação limitada a CircuitoNE e CircuitoNE-QA: `Checks:write`, `Pull requests:write`, `Contents:read`, `Metadata:read`. Cada token limita novamente repositório e permissões. **Não pode escrever testes, workflows ou `accepted`**; sua chave fica no environment `QA Publisher`, mas ainda há uma cópia local até concluir a #31. |
| Workflows do GitHub Actions | CI de PR sem segredo privilegiado; QA confiável prepara/promove a suíte e limpa branches; o publicador emite o check. | Os jobs recebem permissões delimitadas. Escrita em conteúdo do QA usa `GITHUB_TOKEN` temporário somente nos jobs confiáveis em `main`; o job que testa código candidato não recebe essa escrita nem a chave do App QA. O environment `QA Publisher` restringe a execução a `main`. |
| `QA_DISPATCH_TOKEN` | Disparar o workflow canônico do QA após o CI de origem. | Token restrito ao repositório QA e `Actions:write`, guardado no environment `QA Dispatch` do CircuitoNE; validade registrada até 23/09/2027. O QA confere run, origem, modo, PR e SHA antes de aceitar o pedido. Não autoriza edição da suíte. |
| Dependabot, CodeQL e Codecov | Propor dependências, analisar código e receber cobertura, respectivamente. | Não aprovam PRs nem releases. Codecov recebe LCOV via OIDC, sem `CODECOV_TOKEN`; status de cobertura é informativo. Upgrade continua sujeito a review, CI e homologação. |

A consulta às instalações nesta data retornou para `ignisdevne`: `Contents:write`, `Pull requests:write`, `Issues:write`, `Checks:read`, `Statuses:read`, `Actions:write`, `Workflows:write`, `Members:read` e `Metadata:read`. `Actions:write` e `Workflows:write` foram concedidos temporariamente para o bootstrap; `Members:read` não é pedido pelo helper e deve ter sua necessidade revista. **A chave local pode emitir um token com essas permissões instaladas**, mesmo que o helper cotidiano peça menos. A seleção atual de apenas um repositório preserva a exclusão do QA; para atender outros repositórios da organização, adicioná-los individualmente sem incluir o QA.

## Ambientes e dados

- `CircuitoNE-dev` e `CircuitoNE` estão na mesma organização Supabase `IgnisDev`, cujo plano Free foi confirmado; ambos estão em São Paulo desde a substituição da produção vazia em 22/09/2026. Papéis humanos por projeto não estão disponíveis nesse plano: papéis concedidos na organização alcançam seus projetos. Não prometer acesso humano apenas à homologação sem trocar de plano ou separar organizações ([Supabase Access Control](https://supabase.com/docs/guides/platform/access-control)); revisar essa condição antes de adicionar colaboradores, junto da [#43](https://github.com/IgnisDevNE/CircuitoNE/issues/43). O conector Supabase do Codex autenticado com a conta pessoal criou o novo projeto de produção após confirmação; essa autoridade deve ser desconectada do harness ou substituída por identidade isolada antes de liberar agentes implementadores.
- Cada environment GitHub **deve** guardar somente as credenciais do respectivo projeto. Homologação pode acessar `CircuitoNE-dev`; produção acessa `CircuitoNE` somente após gate humano. O job atual de homologação lê credenciais, mas não migra nem faz deploy. Credenciais de produção nunca entram em PR, preview ou runner que execute código candidato. O App implementador recebeu HTTP 403 ao ler variables/secrets. Por autorização expressa do responsável, a conta humana atualizou e conferiu em 23/09/2026 UTC as três variáveis de produção (ref, URL e chave publicável) e removeu o secret de senha do banco antigo. A nova senha e sua validação seguem pendentes na #43; essa exceção não amplia o App nem dispensa o gate humano.
- O frontend pode receber somente URL e **chave publicável**. Chave secreta ou `service_role` fica em componente servidor controlado, se necessário, com autorização própria; ela contorna RLS e nunca vai ao navegador, ao repositório ou ao teste de PR ([Supabase API keys](https://supabase.com/docs/guides/getting-started/api-keys)). O projeto ainda é protótipo sem integração real com Auth/RLS.
- O Podman neste Windows é preview, não produção. Nenhuma identidade de deploy do futuro Debian foi criada por este controle. Antes de implantar, definir usuário de serviço rootless e credencial de deploy separada das contas GitHub/Supabase administrativas, com acesso apenas ao ambiente correspondente.

## Conceder, usar e retirar acesso

1. Registrar na issue/PR o responsável, a finalidade, o repositório/projeto, a permissão mínima, a duração e quem revisará. Mudança de instalação de App, workflow, proteção ou environment passa por PR/revisão e por verificação do efeito real antes de ser declarada ativa. O [controle de mudanças](change-control.md) define os gates de cada entrega.
2. Emitir credenciais por job/comando com escopo de repositório e permissão, nunca por padrão com todo o poder da instalação. Não guardar token em arquivo ou log, não copiar segredo para `temp/` ou para artefatos. O autor de uma PR não é seu aprovador; um check do App implementador não substitui o check do App QA.
3. Antes de agentes implementarem regras reais, retirar `Actions:write` e `Workflows:write` temporários do App implementador, revisar `Members:read`, retirar chaves privadas, credenciais humanas e conexão Supabase da conta pessoal do ambiente acessível ao harness e rotacionar o que tiver sido exposto. Manter um emissor de tokens fora desse ambiente; reduzir o helper local, sozinho, não basta. Evidência e negativas ficam na #31.
4. Revisar instalações, membros, environments e tokens a cada gate de fase e após qualquer mudança de papel. Em perda de chave ou suspeita de uso indevido, revogar a credencial, rotacionar os segredos afetados, examinar logs/PRs/checks e registrar incidente e recuperação em issue privada ou canal apropriado, sem publicar valores secretos.

O QA privado no plano atual não oferece proteção nativa de branch. Revisão humana de suas PRs é um controle operacional; quem tiver escrita administrativa ou credenciais humanas nesse ambiente ainda pode alterá-lo. Uma PR aberta pelo App QA com branch publicada pela conta humana não prova independência entre quem publicou e quem aprovou. A #31 permanece aberta até essa limitação, as credenciais locais e os testes negativos serem tratados. Não usar o resultado verde do CI como prova de que a separação de autoridade já está completa.
